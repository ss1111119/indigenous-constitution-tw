## 1. 對照實作與規格

- [x] 1.1 確認 "Refreshed data is committed only after validation passes" 新增的「無可比基準」Scenario 與轉檔腳本的實際分支完全一致：兩種略過條件（processed 縣市人口檔不存在、由該檔導出的原住民合計不大於 0）、略過時輸出可辨識原因、略過幅度比對不影響加總自我驗證。驗證方式：逐列對照 delta spec 的 baseline conditions 表與 scripts/build-population.ps1 幅度檢查區塊的三條分支，把對照結果記在本任務完成註記；若出現任何不一致，停下來回報，不得為了對齊規格而修改實作。
  - 對照結果：三列全部一致。檔案不存在 → 幅度檢查區塊末的 else 分支輸出「找不到…（首次建置）」；
    導出合計不大於 0 → 讀檔後的前置判斷輸出「原住民合計為 0，無法作為基準」；
    合計為正 → 進入 deltaPct 計算並分流至通過／拒絕／具名放行。
    導出方式為累加前一期 `data` 陣列的 `indigenous_total`，非讀取現成全國欄位，與規格措辭相符。
    加總自我驗證在幅度檢查區塊之前即以 throw 中止，略過分支不回頭影響它。實作未修改。

- [x] 1.2 實地證明略過路徑不中止且會說明原因：在暫存目錄複製一份必要輸入，移除 data/processed/population-by-county.json 後執行 `pwsh scripts/build-population.ps1 -Period 11507`，確認輸出含「幅度檢查略過」字樣並指出是首次建置、程序以成功結束、且產出檔仍通過加總自我驗證。驗證目標：該次執行的 stdout 與離開碼。

## 2. 操作文件補正

- [x] 2.1 README 的刷新章節讀完後，讀者能知道存在一條幅度檢查不執行的路徑及其兩種觸發條件。驗證方式：內容審閱，確認新增段落與 delta spec 的 Scenario 敘述一致，且未宣稱該路徑可由操作者主動觸發。

- [x] 2.2 README 兩處在無基準時不成立的絕對敘述改為與規格一致：「指定期別只跳過探測，不跳過任何驗證」與排程「做得到自我驗證與幅度檢查」。驗證方式：內容審閱，確認改寫後的句子在「有基準」與「無基準」兩種情況下都為真，且未削弱既有的「放行只鬆綁幅度檢查」語意。
  - 實際改寫四處，非任務原先列的兩處：覆核時另外查出「幅度檢查被擋下時」開頭句
    （轉檔會在變動超過 ±1% 時中止）與本機指令註解（含自我驗證與幅度檢查）同樣寫成無條件成立，
    一併補上「有可比對基準時」的前提。「指定期別」那句改為分開陳述自我驗證與幅度比對，
    避免讀者把因果解讀成「任何情況下兩者都會跑」。

## 3. 回歸確認

- [x] 3.1 既有驗證行為未因文件與規格變更而改變：執行 `pwsh tests/run-regression.ps1`，確認幅度檢查的通過、拒絕、具名放行三條既有路徑結果與變更前相同。驗證目標：該腳本的離開碼與各案例結果摘要。
